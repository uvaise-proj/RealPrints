DATABASE_URL=postgresql://postgres:password@localhost:5432/realprints
APP_ENV=development
LOG_LEVEL=INFO
APP_HOST=0.0.0.0
APP_PORT=8000

# Auth — generate a strong secret: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-me-before-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480
